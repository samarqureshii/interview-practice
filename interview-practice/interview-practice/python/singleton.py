# A singleton is a design pattern that restricts a class to a single instance.
# Implement a Singleton class in Python. 
# Ensure that only one instance of the class can be created and provide a method to retrieve this instance.